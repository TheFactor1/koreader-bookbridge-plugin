local _ = require("gettext")
return {
    fullname = _("Bookbridge"),
    description = _([[Connects KOReader to your reading stack: request books through Shelfmark, keep the library in step with Calibre-Web-Automated, track reading on Hardcover, search Anna's Archive.]]),
}
